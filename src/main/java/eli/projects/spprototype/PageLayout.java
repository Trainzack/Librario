package eli.projects.spprototype;
/**
 * This class is a list of images and text (with associated positioning/font information) 
 * that we can turn directly into whatever kind of output we need for this page.
 * 
 * This object should have all of the information it needs to make its output, without having to reference any other objects.
 * 
 * Then, we will have several methods to make the output in various kinds of formats.
 * 
 * @author Eli
 *
 */
public class PageLayout {
	// Probably we want methods that we can call on the plan to add text and images
	
	// Then, we need a method that can export this as a PDPage (or add such a page to a document)
	
}
